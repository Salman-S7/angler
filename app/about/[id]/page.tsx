import { useRouter } from 'next/navigation'
import React from 'react'

const page = () => {
    const router = use();
    const { id } = ;
  return (
      <div>Page with some id { id }</div>
  )
}

export default page